# Caça-Palavras Evangélico (React + Vite + Tailwind)

## Rodar local
```bash
npm install
npm run dev
```

## Build (para Netlify)
```bash
npm run build
```
A saída fica em `dist/`.

## Deploy no Netlify (grátis)
- Build command: `npm run build`
- Publish directory: `dist`
